import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
const mount=document.querySelector('#webgl');const scene=new THREE.Scene();scene.fog=new THREE.FogExp2(0x05060d,.055);
const camera=new THREE.PerspectiveCamera(45,innerWidth/innerHeight,.1,100);camera.position.set(0,0,8.8);
const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.8));renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.15;mount.appendChild(renderer.domElement);
const group=new THREE.Group();scene.add(group);
const sphere=new THREE.Mesh(new THREE.IcosahedronGeometry(2.15,18),new THREE.MeshPhysicalMaterial({color:0x12072e,roughness:.19,metalness:.35,transmission:.08,clearcoat:1,clearcoatRoughness:.12,iridescence:.65,iridescenceIOR:1.45}));sphere.position.set(2.55,.05,0);group.add(sphere);
const wire=new THREE.Mesh(new THREE.IcosahedronGeometry(2.19,5),new THREE.MeshBasicMaterial({color:0x8e6cff,wireframe:true,transparent:true,opacity:.055}));wire.position.copy(sphere.position);group.add(wire);
const halo=new THREE.Mesh(new THREE.SphereGeometry(2.42,64,64),new THREE.MeshBasicMaterial({color:0x6b35ff,transparent:true,opacity:.035,side:THREE.BackSide}));halo.position.copy(sphere.position);group.add(halo);
scene.add(new THREE.AmbientLight(0x251249,1.2));const key=new THREE.PointLight(0x8e6cff,120,18,2);key.position.set(4,2.5,4);scene.add(key);const rim=new THREE.PointLight(0x7ccfff,95,16,2);rim.position.set(1,-3,-2);scene.add(rim);const dark=new THREE.PointLight(0x361088,60,12,2);dark.position.set(5,-1,1);scene.add(dark);
const count=900,pos=new Float32Array(count*3);for(let i=0;i<count;i++){const r=8+Math.random()*15,a=Math.random()*Math.PI*2,z=(Math.random()-.5)*18;pos[i*3]=Math.cos(a)*r;pos[i*3+1]=Math.sin(a)*r;pos[i*3+2]=z}const pg=new THREE.BufferGeometry();pg.setAttribute('position',new THREE.BufferAttribute(pos,3));const points=new THREE.Points(pg,new THREE.PointsMaterial({color:0x8e6cff,size:.018,transparent:true,opacity:.5}));scene.add(points);
let mx=0,my=0,scroll=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5)*2;my=(e.clientY/innerHeight-.5)*2});addEventListener('scroll',()=>scroll=scrollY/(document.documentElement.scrollHeight-innerHeight));
function tick(t){const s=Math.min(scroll,1);sphere.rotation.y=t*.00013+mx*.09;sphere.rotation.x=t*.00005+my*.06;wire.rotation.copy(sphere.rotation);halo.scale.setScalar(1+Math.sin(t*.001)*.015);group.position.x=-s*1.8;group.scale.setScalar(1+s*2.5);camera.position.z=8.8-s*3.2;camera.position.x+=((mx*.18)-camera.position.x)*.025;camera.position.y+=((-my*.12)-camera.position.y)*.025;points.rotation.z=t*.000008;renderer.render(scene,camera);requestAnimationFrame(tick)}requestAnimationFrame(tick);
addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(Math.min(devicePixelRatio,1.8))});
